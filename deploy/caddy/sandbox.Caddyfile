sandbox.fisconexa.com.br {
  encode zstd gzip
  request_body {
    max_size 128KB
  }
  import /etc/caddy/fisconexa-docs.caddy
  handle {
    reverse_proxy 127.0.0.1:9001
  }
  header {
    -Server
    X-Content-Type-Options "nosniff"
    Referrer-Policy "strict-origin-when-cross-origin"
  }
}
