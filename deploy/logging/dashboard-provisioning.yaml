apiVersion: 1
providers:
  - name: fisconexa
    folder: FiscoNexa
    folderUid: fisconexa
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

